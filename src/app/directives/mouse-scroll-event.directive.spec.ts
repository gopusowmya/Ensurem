/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MouseScrollEventDirective } from './mouse-scroll-event.directive';

describe('Directive: MouseScrollEvent', () => {
  it('should create an instance', () => {
    const directive = new MouseScrollEventDirective();
    expect(directive).toBeTruthy();
  });
});
